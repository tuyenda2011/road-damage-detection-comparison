# Ultralytics dataset config for Road Damage Detection.
# Update path if data/processed is moved elsewhere.
path: data/processed
train: images/train
val: images/val
test: images/test

names:
  0: D00
  1: D10
  2: D20
  3: D40
